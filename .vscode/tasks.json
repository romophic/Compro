{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "C/C++: clang++-6.0 build active file",
      "command": "ccache clang++ -std=c++17 main.cpp -o main; echo -----Build Complete-----; ./main; rm -f main",
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": [
        "$gcc"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}